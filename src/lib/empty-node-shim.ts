// Stub for Node built-ins that libpg-query's emscripten bundle references but
// never actually needs in the browser (guarded by runtime environment checks
// the bundler can't statically eliminate). See next.config.ts turbopack.resolveAlias.
export {};
